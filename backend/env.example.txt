# ZenTea License Server 配置
# 复制此文件为 .env 并修改配置

# 数据库
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/zentea_license

# JWT 密钥（生产环境必须修改！）
SECRET_KEY=your-super-secret-key-change-in-production

# 管理员账户
ADMIN_USERNAME=admin
ADMIN_PASSWORD=please-set-strong-password-in-production
ADMIN_EMAIL=admin@example.com

# 运行环境（development / production）
ENV=development

# CORS 白名单（JSON 数组或逗号分隔）
BACKEND_CORS_ORIGINS=["http://localhost:3001","http://localhost:3002"]
# 是否允许携带 cookie（本项目使用 Bearer Token，默认 false）
CORS_ALLOW_CREDENTIALS=false

# 授权服务器地址（对外公开的地址）
LICENSE_SERVER_URL=https://sq.jinghuatea.com

# 调试模式
DEBUG=false

